# replace "database" with full path of database folder
%define DATADIR database

<zodb>
    <blobstorage>
        blob-dir $DATADIR/blobs
      <filestorage>
          path $DATADIR/generic
      </filestorage>
    </blobstorage>
</zodb>
